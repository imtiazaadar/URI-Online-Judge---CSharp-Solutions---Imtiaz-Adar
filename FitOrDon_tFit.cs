using System;
using System.Collections.Generic;
using System.Text;

namespace ImtiazAdar
{
    class FitOrDon_tFit
    {
        //static void Main(string[] args)
        //{
        //    int N = int.Parse(Console.ReadLine());
        //    while(N-- > 0)
        //    {
        //        string[] sen = Console.ReadLine().Split(' ');
        //        string firstsen = sen[0];
        //        string secondsen = sen[1];
        //        int index = firstsen.LastIndexOf(secondsen[0]);
        //        int count = 0;
        //        if (firstsen.Length < secondsen.Length)
        //        {
        //            Console.WriteLine("nao encaixa");
        //        }
        //        else
        //        {
        //            for (int i = firstsen.Length - 1, j = secondsen.Length - 1; i >= 0 && j >= 0; i--, j--)
        //            {
        //                if (firstsen[i] != secondsen[j]) break;
        //                count++;
        //            }
        //            if (count == secondsen.Length)
        //                Console.WriteLine("encaixa");
        //            else Console.WriteLine("nao encaixa");
        //        }
        //    }
        //}
    }
}
