using System;
using System.Collections.Generic;
using System.Text;
using System.Linq;

namespace ImtiazAdar
{
    class CountingCharacters
    {
        //static void Main(string[] args)
        //{
        //    string sen;
        //    List<string> str = new List<string>();
        //    while ((sen = Console.ReadLine()) != null)
        //    {
        //        if (sen == "0")
        //        {
        //            Console.WriteLine();
        //            str.Sort();
        //            foreach (var it in str.OrderByDescending(i => i.Length))
        //            {
        //                Console.WriteLine($"The biggest word: {it}");
        //                break;
        //            }                    
        //            break;
        //        }
        //        else
        //        {
        //            string[] newsen = sen.Split(' ');
        //            for (int i = 0; i < newsen.Length - 1; i++)
        //            {
        //                str.Add(newsen[i]);
        //                Console.Write($"{newsen[i].Length}-");
        //            }
        //            str.Add(newsen[newsen.Length - 1]);
        //            Console.WriteLine($"{newsen[newsen.Length - 1].Length}");
        //        }
        //    }
        //}
    }
}
