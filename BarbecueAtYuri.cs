using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace ImtiazAdar
{
    class BarbecueAtYuri
    {
        //static void Main(string[] args)
        //{
        //    string how;
        //    while((how = Console.ReadLine()) != null)
        //    {
        //        int N = Convert.ToInt32(how);
        //        Dictionary<string, int> dic = new Dictionary<string, int>();
        //        for(int i = 0; i < N; i++)
        //        {
        //            string[] sen = Console.ReadLine().Split(' ');
        //            string newsen = sen[0];
        //            int val = Convert.ToInt32(sen[1]);
        //            dic[newsen] = val;
        //        }
        //        StringBuilder sb = new StringBuilder();
        //        foreach (var item in dic.OrderBy(i => i.Value))
        //        {
        //            sb.Append(item.Key + " ");
        //        }
        //        sb.Length--;
        //        Console.WriteLine(sb);
        //    }
        //}
    }
}
