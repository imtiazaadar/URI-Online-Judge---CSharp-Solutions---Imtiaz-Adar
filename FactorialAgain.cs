using System;
using System.Collections.Generic;
using System.Text;

namespace ImtiazAdar
{
    class FactorialAgain
    {
        //static void Main(string[] args)
        //{
        //    string sen;
        //    int[] arr = {1, 2, 6, 24, 120};
        //    while((sen = Console.ReadLine()) != "0")
        //    {
        //        char[] chr = sen.ToCharArray();
        //        string newsen = "";
        //        for (int i = chr.Length - 1; i >= 0; i--)
        //            newsen += chr[i];
        //        int total = 0;
        //        for(int i = 0; i < newsen.Length; i++)
        //        {
        //            total += (newsen[i] - '0') * arr[i];
        //        }
        //        Console.WriteLine(total);
        //    }
        //}
    }
}
