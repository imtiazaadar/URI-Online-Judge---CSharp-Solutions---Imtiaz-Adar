using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace ImtiazAdar
{
    class AndyDictionary
    {
        //static void Main(string[] args)
        //{
        //    string ss = "";
        //    string sen;
        //    while((sen = Console.ReadLine()) != "0")
        //    {
        //        sen = sen.ToLower();
        //        for(int i = 0; i < sen.Length; i++)
        //        {
        //            if((int)sen[i] < 97 || (int)sen[i] > 122)
        //            {
        //                sen = sen.Replace(sen[i], ' ');
        //            }
        //        }
        //        ss += sen + ' ';
        //    }
        //    string[] li = ss.Split(' ');
        //    Dictionary<string, int> dic = new Dictionary<string, int>();
        //    for(int i = 0; i < li.Length; i++)
        //    {
        //        if (dic.ContainsKey(li[i]))
        //        {
        //            dic[li[i]] += 1;
        //        }
        //        else dic[li[i]] = 1;
        //    }
        //    foreach(var items in dic.OrderBy(i => i.Key))
        //    {
        //        Console.WriteLine(items.Key);
        //    }
        //}
    }
}
