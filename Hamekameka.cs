using System;
using System.Collections.Generic;
using System.Text;

namespace ImtiazAdar
{
    class Hamekameka
    {
        //static void Main(string[] args)
        //{
        //    int N = int.Parse(Console.ReadLine());
        //    while(N-- > 0)
        //    {
        //        string[] sen = Console.ReadLine().Split("me", StringSplitOptions.RemoveEmptyEntries);
        //        string first = sen[0];
        //        string second = sen[1];
        //        int fc = 0, sc = 0;
        //        for(int i = 0; i < first.Length; i++)
        //        {
        //            if (first[i] == 'a') fc++;
        //        }
        //        for(int i = 0; i < second.Length; i++)
        //        {
        //            if (second[i] == 'a') sc++;
        //        }
        //        string finalsen = "k";
        //        for (int i = 0; i < fc * sc; i++)
        //            finalsen += 'a';
        //        Console.WriteLine(finalsen);
        //    }
        //}
    }
}
